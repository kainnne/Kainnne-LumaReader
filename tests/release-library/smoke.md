# Windows release smoke test

This document verifies that the packaged application can select a library, scan Markdown, and open a document through its loopback-only reader service.

**Status:** ready for LumaReader 1.0.0.
